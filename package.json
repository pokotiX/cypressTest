{
  "name": "cypress-weather-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for weather functionality using Cypress",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "npm run cy:run"
  },
  "private": true,
  "devDependencies": {
    "cypress": "^14.0.3"
  }
}